[workspace]
changelog_config = "cliff.toml"
allow_dirty = false

[[package]]
name = "rar-stream"
changelog_update = true
# Disable release-plz publishing - build.yml handles npm, crates.io, and GitHub releases on tag push
git_release_enable = false
git_tag_enable = false
publish = false
